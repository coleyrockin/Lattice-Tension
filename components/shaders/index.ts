import edgeVertexShader from './edge.vertex.glsl';
import edgeFragmentShader from './edge.fragment.glsl';
import nodeVertexShader from './node.vertex.glsl';
import nodeFragmentShader from './node.fragment.glsl';
import flowVertexShader from './flow.vertex.glsl';
import flowFragmentShader from './flow.fragment.glsl';
import nebulaVertexShader from './nebula.vertex.glsl';
import nebulaFragmentShader from './nebula.fragment.glsl';

export {
  edgeVertexShader,
  edgeFragmentShader,
  nodeVertexShader,
  nodeFragmentShader,
  flowVertexShader,
  flowFragmentShader,
  nebulaVertexShader,
  nebulaFragmentShader,
};